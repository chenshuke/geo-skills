# GEO Skills Suite Python dependencies
# Required for API and runtime diagnostics
requests>=2.28.0
python-dotenv>=1.0.0

# Optional dependencies (install only when needed)
# Local cover generation: python3 -m pip install Pillow
# Pillow>=10.0.0
# Feishu/Bitable sync: python3 -m pip install baseopensdk
# baseopensdk>=0.2.0
